[bitmex]
apiKey=
secret=

[sqlite3]
; root path is the current directory which is running .py file.
db_path=


[influxdb]
; you need to create database and user manually at first.
    host=127.0.0.1
    port=8086
    username=
    password=
    db_name=tradingbot